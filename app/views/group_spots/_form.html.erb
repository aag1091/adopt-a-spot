<%= form_for(@group_spot) do |f| %>
  <% if @group_spot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group_spot.errors.count, "error") %> prohibited this group_spot from being saved:</h2>

      <ul>
      <% @group_spot.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :group_id %><br>
    <%= f.text_field :group_id %>
  </div>
  <div class="field">
    <%= f.label :spot_id %><br>
    <%= f.text_field :spot_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
